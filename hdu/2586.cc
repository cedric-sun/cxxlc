#include <cstdio>

const int N=40000+5;
int n,m;
bool deg[N];

int main()
{
    int T;
    scanf("%d",&T);
    while (T--)
    {
        scanf("%d %d",&n,&m);
        for (int i=0;i<n-1;i++)
    }
}
