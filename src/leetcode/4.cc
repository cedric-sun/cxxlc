// cesun, 9/8/20 2:52 PM.

#include <vector>

using std::vector;

// for array x of size odd n ([0,n-1]), median is x[n>>1]
// for array x of size even n ([0,n-1]), median is (x[n>>1]+x[(n>>1)-1])/2

class Solution {
public:
    double findMedianSortedArrays(const vector<int> &A, const vector<int> &B) {

    }
};